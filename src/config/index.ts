import keys from './keys.config';
import intervals from './intervals.config';
import constants from './constants.config';
import paths from './paths.config';
import tokens from './tokens.config';

export default {
    keys,
    intervals,
    constants,
    paths,
    tokens,
};
